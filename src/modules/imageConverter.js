let Config = require('../../config.json');
const ImageParser = require("image-parser");

var imageConverter = (image /* Requires to be a buffer */) => {

  

  return
}

module.exports = imageConverter